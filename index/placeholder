hey some noticed issues that arent persay big but just wanted to mention if ya notice the chunk ID isnt accurate because it gets a new id when split and when merged so the ids are out of order and these are both what I got for running the other scripts on moby dick.
